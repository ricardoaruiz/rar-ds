{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.5.6"
  },
  "dependencies": {
    "@radix-ui/react-checkbox": "^1.0.1",
    "@rar-ui/react": "file:packages/react"
  }
}
